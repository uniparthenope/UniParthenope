import { View } from 'tns-core-modules/ui/core/view';
import { AccordionBase } from './accordion.common';
export declare class Accordion extends AccordionBase {
    private _listAdapter;
    private _previousGroup;
    private _androidViewId;
    _realizedItems: Map<globalAndroid.view.View, View>;
    _realizedHeaderTemplates: Map<string, Map<globalAndroid.view.View, View>>;
    _realizedItemHeaderTemplates: Map<string, Map<globalAndroid.view.View, View>>;
    _realizedItemContentTemplates: Map<string, Map<globalAndroid.view.View, View>>;
    _realizedFooterTemplates: Map<string, Map<globalAndroid.view.View, View>>;
    _itemsMap: Map<any, any>;
    _headerMap: Map<any, any>;
    _footerMap: Map<any, any>;
    _expandedViews: Map<any, any>;
    constructor();
    expandAll(): void;
    collapseAll(): void;
    readonly android: any;
    createNativeView(): globalAndroid.widget.ExpandableListView;
    initNativeView(): void;
    disposeNativeView(): void;
    eachChildView(callback: (child: View) => boolean): void;
    refresh(): void;
    updateNativeIndexes(oldIndexes: any, newIndexes: any): void;
    updateNativeItems(oldItems: any, newItems: any): void;
    _selectedIndexUpdatedFromNative(newIndex: number[]): void;
    itemExpanded(index: number): void;
    itemCollapsed(index: number): void;
    expandItem(id: number): void;
    collapseItem(id: number): void;
    itemIsExpanded(id: number): boolean;
    private clearRealizedCells;
}
